on: [push]

jobs:
  package_debian:
    runs-on: ubuntu-latest
    name: Job to build debian packages
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build
        uses: ./.github/actions/
        id: build

